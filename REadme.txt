To run this either import it in eclipse as a maven project or use the command line with MAVEN commands to compile it

cp to the metrics folder in the repo so that if you do ls command you will see the pom.xml in the listed files
do a mvn install
this will run the tests and build the jar
to run the  jar which is located in the target folder do a 

java -jar the-jar-name.jar

